export interface Producto {
    id: string;          // El ID único generado por la base de datos
    nombre: string;      // El nombre del producto
    precio: number;      // El precio del producto
    categoria: string;   // La categoría del producto
  }
  