export interface Envio {
    id: string;          // ID único generado para el envío
    direccion: string;   // Dirección del envío
    fechaEnvio: string;  // Fecha en que se realiza el envío (en formato ISO)
  }
  